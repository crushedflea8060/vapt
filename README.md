# VAPT (Visual Apt)
 - This program accepts no arguments, but it requires root access to install packages.
  - This program is designed to be modified for other package managers, please feel free to replicate this.
# Compiling and Running
 - `cargo run` will run it straight from the directory.
  - `cargo build` will get you a binary.